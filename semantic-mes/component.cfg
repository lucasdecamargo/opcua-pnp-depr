
logging = {
      level = {
          app = "info"
          opcua = "error"
      }
      path = "logs"
}


// OPC UA Server configuration
opcua = {
    port = 4840
    encryption = false
    anonymous = true
}

// We do not register with any LDS
lds-uri = "disabled"

pnp = { }
