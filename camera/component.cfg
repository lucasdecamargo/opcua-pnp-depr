logging = {
      level = {
          app = "info"
          opcua = "error"
      }
      path = "logs"
}

// OPC UA Server configuration

opcua = {
    port = 4842
    encryption = true
    anonymous = false
}

lds-uri = "pnp.component.mes"

camera = { }